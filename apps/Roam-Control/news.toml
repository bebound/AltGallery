# render_news.py config: python3 templates/render_news.py --out apps/Roam-Control
# Colors left unset are auto-derived: tint from config.toml, background as
# a light shade of the icon's dominant color.

name = "Roam Control"
tagline = "Simulate & test iPhone location"

# Color notes — each key below controls one element of the image. All are
# optional: unset keys are auto-derived. Shown values are the current result.
[colors]
# tint = "#1090F0"          # NEW UPDATE badge + top-left glow (default: [app] tint_color)
# tint_alt = "#1090F0"      # bottom-right glow (default: [source] tint_color)
# background = "#D6E4F0"    # gradient base (derived from the icon's dominant color)
# bg_mid = "#E7EEF3"        # gradient mid stop (derived from background)
# bg_dark = "#BBD3DE"       # gradient end stop (derived from background)
# text_color = "#111111"    # app name (auto white/black)
# tagline_color = "#51A0BD" # tagline text (derived from background)
